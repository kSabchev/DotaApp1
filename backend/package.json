{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/backend/src/index.js",
    "ingest": "ts-node --transpile-only src/ingest/opendota.ts",
    "backtest": "ts-node --transpile-only src/backtest/run.ts",
    "train": "ts-node --transpile-only src/model/train.ts",
    "checkmatchups": "ts-node --transpile-only src/checks/itemMatchups.ts",
    "test": "ts-node --transpile-only tests/run.ts",
    "prometa": "ts-node --transpile-only src/model/buildProMeta.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.3",
    "@types/node-fetch": "^2.6.13",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "node-fetch": "^2.7.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^6.0.3"
  }
}
