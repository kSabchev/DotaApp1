# Copy to .env.local for local overrides, or set in your hosting dashboard.
# Backend API base URL. Leave unset for local dev (defaults to http://localhost:3001/api).
# In production, point this at your deployed backend, e.g.:
VITE_API_BASE=https://your-backend.onrender.com/api
